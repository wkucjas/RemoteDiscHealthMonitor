#include "Disc.h"

Disc::Disc(std::string _caption, std::string _deviceId, std::string _model, int _partitions, int _size)
    : caption(_caption), deviceId(_deviceId), model(_model), partitions(_partitions), size(_size)
{
}